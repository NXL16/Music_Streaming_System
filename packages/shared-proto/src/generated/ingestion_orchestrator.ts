// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: ingestion_orchestrator.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { handleUnaryCall, UntypedServiceImplementation } from "@grpc/grpc-js";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";

export const protobufPackage = "ingestion";

export interface ProcessRequest {
  songId: string;
  userId: string;
  storageKey: string;
}

export interface ProcessResponse {
  jobId: string;
  status: string;
}

export const INGESTION_PACKAGE_NAME = "ingestion";

function createBaseProcessRequest(): ProcessRequest {
  return { songId: "", userId: "", storageKey: "" };
}

export const ProcessRequest: MessageFns<ProcessRequest> = {
  encode(message: ProcessRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.songId !== "") {
      writer.uint32(10).string(message.songId);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.storageKey !== "") {
      writer.uint32(26).string(message.storageKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.songId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.storageKey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseProcessResponse(): ProcessResponse {
  return { jobId: "", status: "" };
}

export const ProcessResponse: MessageFns<ProcessResponse> = {
  encode(message: ProcessResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

export interface IngestionOrchestratorClient {
  processSong(request: ProcessRequest): Observable<ProcessResponse>;
}

export interface IngestionOrchestratorController {
  processSong(request: ProcessRequest): Promise<ProcessResponse> | Observable<ProcessResponse> | ProcessResponse;
}

export function IngestionOrchestratorControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ["processSong"];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("IngestionOrchestrator", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("IngestionOrchestrator", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const INGESTION_ORCHESTRATOR_SERVICE_NAME = "IngestionOrchestrator";

export type IngestionOrchestratorService = typeof IngestionOrchestratorService;
export const IngestionOrchestratorService = {
  processSong: {
    path: "/ingestion.IngestionOrchestrator/ProcessSong" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ProcessRequest): Buffer => Buffer.from(ProcessRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ProcessRequest => ProcessRequest.decode(value),
    responseSerialize: (value: ProcessResponse): Buffer => Buffer.from(ProcessResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ProcessResponse => ProcessResponse.decode(value),
  },
} as const;

export interface IngestionOrchestratorServer extends UntypedServiceImplementation {
  processSong: handleUnaryCall<ProcessRequest, ProcessResponse>;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
