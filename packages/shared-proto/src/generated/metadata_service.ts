// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: metadata_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { handleUnaryCall, UntypedServiceImplementation } from "@grpc/grpc-js";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";

export const protobufPackage = "metadata_service";

export interface SeekPoint {
  timestamp: number;
  byteOffset: number;
}

export interface UpdateMetaRequest {
  songId: string;
  duration: number;
  seekPoints: SeekPoint[];
  waveform: number[];
}

export interface GetStreamDataRequest {
  songId: string;
}

export interface StreamDataResponse {
  songId: string;
  duration: number;
  seekPoints: SeekPoint[];
  waveform: number[];
}

export interface EmptyResponse {
}

export const METADATA_SERVICE_PACKAGE_NAME = "metadata_service";

function createBaseSeekPoint(): SeekPoint {
  return { timestamp: 0, byteOffset: 0 };
}

export const SeekPoint: MessageFns<SeekPoint> = {
  encode(message: SeekPoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestamp !== 0) {
      writer.uint32(9).double(message.timestamp);
    }
    if (message.byteOffset !== 0) {
      writer.uint32(16).int64(message.byteOffset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SeekPoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSeekPoint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.timestamp = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.byteOffset = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseUpdateMetaRequest(): UpdateMetaRequest {
  return { songId: "", duration: 0, seekPoints: [], waveform: [] };
}

export const UpdateMetaRequest: MessageFns<UpdateMetaRequest> = {
  encode(message: UpdateMetaRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.songId !== "") {
      writer.uint32(10).string(message.songId);
    }
    if (message.duration !== 0) {
      writer.uint32(17).double(message.duration);
    }
    for (const v of message.seekPoints) {
      SeekPoint.encode(v!, writer.uint32(26).fork()).join();
    }
    writer.uint32(34).fork();
    for (const v of message.waveform) {
      writer.float(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateMetaRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateMetaRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.songId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.duration = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.seekPoints.push(SeekPoint.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag === 37) {
            message.waveform.push(reader.float());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.waveform.push(reader.float());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseGetStreamDataRequest(): GetStreamDataRequest {
  return { songId: "" };
}

export const GetStreamDataRequest: MessageFns<GetStreamDataRequest> = {
  encode(message: GetStreamDataRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.songId !== "") {
      writer.uint32(10).string(message.songId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStreamDataRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStreamDataRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.songId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseStreamDataResponse(): StreamDataResponse {
  return { songId: "", duration: 0, seekPoints: [], waveform: [] };
}

export const StreamDataResponse: MessageFns<StreamDataResponse> = {
  encode(message: StreamDataResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.songId !== "") {
      writer.uint32(10).string(message.songId);
    }
    if (message.duration !== 0) {
      writer.uint32(17).double(message.duration);
    }
    for (const v of message.seekPoints) {
      SeekPoint.encode(v!, writer.uint32(26).fork()).join();
    }
    writer.uint32(34).fork();
    for (const v of message.waveform) {
      writer.float(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamDataResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamDataResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.songId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.duration = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.seekPoints.push(SeekPoint.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag === 37) {
            message.waveform.push(reader.float());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.waveform.push(reader.float());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseEmptyResponse(): EmptyResponse {
  return {};
}

export const EmptyResponse: MessageFns<EmptyResponse> = {
  encode(_: EmptyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EmptyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmptyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

export interface MetadataServiceClient {
  updateTechnicalMeta(request: UpdateMetaRequest): Observable<EmptyResponse>;

  getStreamData(request: GetStreamDataRequest): Observable<StreamDataResponse>;
}

export interface MetadataServiceController {
  updateTechnicalMeta(request: UpdateMetaRequest): Promise<EmptyResponse> | Observable<EmptyResponse> | EmptyResponse;

  getStreamData(
    request: GetStreamDataRequest,
  ): Promise<StreamDataResponse> | Observable<StreamDataResponse> | StreamDataResponse;
}

export function MetadataServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ["updateTechnicalMeta", "getStreamData"];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("MetadataService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("MetadataService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const METADATA_SERVICE_NAME = "MetadataService";

export type MetadataServiceService = typeof MetadataServiceService;
export const MetadataServiceService = {
  updateTechnicalMeta: {
    path: "/metadata_service.MetadataService/UpdateTechnicalMeta" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UpdateMetaRequest): Buffer => Buffer.from(UpdateMetaRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UpdateMetaRequest => UpdateMetaRequest.decode(value),
    responseSerialize: (value: EmptyResponse): Buffer => Buffer.from(EmptyResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): EmptyResponse => EmptyResponse.decode(value),
  },
  getStreamData: {
    path: "/metadata_service.MetadataService/GetStreamData" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetStreamDataRequest): Buffer => Buffer.from(GetStreamDataRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetStreamDataRequest => GetStreamDataRequest.decode(value),
    responseSerialize: (value: StreamDataResponse): Buffer => Buffer.from(StreamDataResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): StreamDataResponse => StreamDataResponse.decode(value),
  },
} as const;

export interface MetadataServiceServer extends UntypedServiceImplementation {
  updateTechnicalMeta: handleUnaryCall<UpdateMetaRequest, EmptyResponse>;
  getStreamData: handleUnaryCall<GetStreamDataRequest, StreamDataResponse>;
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
