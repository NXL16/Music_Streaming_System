{
  "$schema": "https://turborepo.dev/schema.json",
  "globalPassThroughEnv": ["LOCALAPPDATA", "GOCACHE", "HOME_CACHE_MODE", "NEXT_PUBLIC_DISABLE_HOME_CACHE"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**", "bin/**", "*.exe"]
    },
    "lint": {
      "outputs": []
    },
    "format": {
      "outputs": []
    },
    "type-check": {
      "dependsOn": ["^type-check"],
      "outputs": []
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
